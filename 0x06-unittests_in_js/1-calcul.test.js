const calculateNumber = (type, a, b) => {
  const a, const b = Match.round(a), Math.round(b);
  if (type === SUM) => a + b;
  if (type === SUBTRACT) => a - b;
  if (type === DIVIDE) => {
	 if (b == 0) => 'Error';
	 a / b;
  };
};

module.exports = calculateNumber;
